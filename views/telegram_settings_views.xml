<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_telegram_settings" model="ir.ui.view">
        <field name="name">telegram.settings.form</field>
        <field name="model">res.config.settings</field>
        <field name="arch" type="xml">
            <form string="تنظیمات تلگرام">
                <group>
                    <field name="telegram_api_token"/>
                </group>
                <footer>
                    <button string="ذخیره" type="object" name="set_values" class="btn-primary" />
                    <button string="لغو" class="btn-secondary" special="cancel" />
                </footer>
            </form>
        </field>
    </record>

    <record id="telegram_settings_action" model="ir.actions.act_window">
        <field name="name">تنظیمات تلگرام</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_telegram_settings"/>
        <field name="target">new</field>
    </record>
</odoo>
